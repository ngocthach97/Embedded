#ifndef SEARCH_
#define SEARCH_
#include <stdbool.h>

void print_list(int array[], int size);

bool linear_search(int arrary[], int size, int target);

bool binary_search(int arrary[], int size, int target);

#endif