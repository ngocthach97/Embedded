#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

// Get value max in a 2D Matrix
int get_value_max_array2D(int** matrix, int matrixSize, int* matrixColSize, int target)
{
  
}

// Get value min in a 2D Matrix
int get_value_min_array2D(int** matrix, int matrixSize, int* matrixColSize, int target)
{
  
}

// Search a 2D Matrix
bool search_matrix(int** matrix, int matrixSize, int* matrixColSize, int target)
{

}

int main(const int argc, char *argv[])
{
  // FILE *file;
  // file = fopen("data.txt", "r");
  // if (file == NULL)
  // {
  //   printf("no such file./n");
  //   return 0;
  // }
  // fscanf(file, "Row:%d\nColumn:%d\n", &row, &column);
  // printf("%d  %d\n", row, column);
  // int array[row][column];
  // for (int i = 0; i < row; i++)
  // {
  //   for (int j = 0; j < column; j++)
  //   {
  //     fscanf(file, "%d\t", &array[i][j]);
  //   }
  // }
  // for (int i = 0; i < row; i++)
  // {
  //   for (int j = 0; j < column; j++)
  //   {
  //     printf("%d \t", array[i][j]);
  //   }
  //   printf("\n");
  // }
  // fclose(file);

  printf("%d \n", 2);
  return 0;
}